# This folder holds the Vite project source, not the served site.
# Block direct HTTP access regardless of what cPanel's deploy step does.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
